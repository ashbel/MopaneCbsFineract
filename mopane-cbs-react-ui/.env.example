# Fineract API base URL (no trailing slash). In dev with proxy, use relative path.
VITE_BASE_API_URL=https://localhost:8443/fineract-provider/api/v1
# Tenant identifier for all API requests
VITE_TENANT_IDENTIFIER=default
