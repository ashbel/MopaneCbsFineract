# Classic Fineract seeds tenant_server_connections with root/mysql.
# Keep these in sync unless you run the fixup profile after restore.
MYSQL_ROOT_PASSWORD=mysql
MYSQL_USER=mifos
MYSQL_PASSWORD=mysql
JAVA_OPTS="-Xms512m -Xmx1536m"
